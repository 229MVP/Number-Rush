# Public Supabase keys only — safe for the browser admin client.
# Privileged secrets belong only in Edge Functions / server env, never NEXT_PUBLIC_*.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
