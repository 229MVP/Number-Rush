/** Local AsyncStorage player document schema revision. */
export const LOCAL_PLAYER_SCHEMA_VERSION = 1;

/** Cloud `player_*` tables schema revision expected by the client. */
export const CLOUD_PLAYER_SCHEMA_VERSION = 1;
