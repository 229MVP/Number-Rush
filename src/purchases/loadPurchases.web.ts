import type { PurchasesModuleShape } from './purchasesModuleTypes';

/** Web stub — never loads react-native-purchases. */
export function loadPurchasesSdk(): PurchasesModuleShape | null {
  return null;
}
